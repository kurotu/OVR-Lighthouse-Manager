<Page
    x:Class="OVRLighthouseManager.Views.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:OVRLighthouseManager.ViewModels"
    mc:Ignorable="d">

    <Page.Resources>
        <DataTemplate x:Key="ContactListViewTemplate" x:DataType="models:LighthouseListItemViewModel">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <FontIcon
                    Grid.RowSpan="2"
                    Glyph="{x:Bind Glyph, Mode=OneWay}"
                    Width="32"
                    Height="32"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center"
                    Foreground="{ThemeResource AccentFillColorDefaultBrush}"
                />

                <TextBlock Grid.Column="1"
                    Text="{x:Bind Name}"
                    x:Phase="1"
                    Style="{ThemeResource BaseTextBlockStyle}"
                    Margin="12,6,0,0"/>
                <TextBlock  Grid.Column="1"
                    Grid.Row="1"
                    Text="{x:Bind BluetoothAddress}"
                    x:Phase="2"
                    Style="{ThemeResource BodyTextBlockStyle}"
                    Margin="12,0,0,6"/>
                <Button
                    Grid.Column="2"
                    Grid.RowSpan="2"
                    Style="{StaticResource TextBlockButtonStyle}"
                    >
                    <Button.Content>
                        <FontIcon
                            Glyph="&#xE712;"
                            Width="32"
                            Height="32"
                            VerticalAlignment="Center"
                            HorizontalAlignment="Center"
                            />
                    </Button.Content>
                    <Button.Flyout>
                        <MenuFlyout Placement="Bottom">
                            <MenuFlyoutItem
                                Text="Power On"
                                Command="{x:Bind ClickPowerOn}"/>
                            <MenuFlyoutItem
                                Text="Sleep"
                                Command="{x:Bind ClickSleep}"/>
                            <MenuFlyoutItem
                                Text="Standby"
                                Command="{x:Bind ClickStandby}"/>
                            <MenuFlyoutSeparator />
                            <MenuFlyoutItem
                                Text="Remove"
                                Command="{x:Bind ClickRemoveDeviceCommand}"
                                />
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
            </Grid>
        </DataTemplate>
    </Page.Resources>

    <Grid x:Name="ContentArea">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0">
            <ToggleSwitch
                IsOn="{x:Bind ViewModel.PowerManagement, Mode=OneWay}"
                Toggled="{x:Bind ViewModel.OnTogglePowerManagement}"
                >
                <ToggleSwitch.Header>
                    <TextBlock Text="Manage Base Stations" />
                </ToggleSwitch.Header>
            </ToggleSwitch>
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.Column="0"
                    Orientation="Horizontal"
                    Spacing="12"
                    >
            <TextBlock Text="Base Stations" VerticalAlignment="Center" />
            <Button
                Grid.Column="0"
                Grid.Row="1"
                x:Name="ScanButton"
                Content="Scan"
                Command="{x:Bind ViewModel.ClickScanCommand}"
                IsEnabled="{x:Bind ViewModel.CanStartScan, Mode=OneWay}"
            />
        </StackPanel>

        <ListView
            Grid.Column="0"
            Grid.Row="2"
                x:Name="BaseStationListView"
                ItemsSource="{x:Bind ViewModel.Devices, Mode=OneWay}"
                IsItemClickEnabled="True"
                SelectionMode="None"
                ItemTemplate="{StaticResource ContactListViewTemplate}"
                ItemClick="{x:Bind ViewModel.OnClickDevice}"
            >
        </ListView>
    </Grid>
</Page>
